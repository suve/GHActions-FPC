name: 'Free Pascal Compiler'
description: 'Compile Pascal code using FPC'
author: 'suve'
inputs:
  fpc:
    name: 'Full path to fpc executable'
    required: false
    default: ''
  flags:
    name: 'FPC flags (space-separated)'
    required: false
    default: ''
  source:
    name: 'Main source file to be compiled'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
